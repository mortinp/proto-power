-How to set min time interval in zoomable stock chart; min time interval bigger than set; mm time interval sets to 10 min; set max interval for bullets; set intervals as provided in chart data. DONE (categoryAxesSettings.maxSeries = 0;).

-show and hide events (look in events API).DONE (a palo!!!).

-Object-Oriented javascript. DONE.

-Upload file through ajax. DONE.

-Hide jquery ui tabs items at the beginning of the load (before the tabs are created). DONE (create tabs at the beginning and recreate later).

-format dates in javascript. DONE.

-prevent node renaming/deletion/etc in jstree. DONE (with rules, but doesn't work well).
